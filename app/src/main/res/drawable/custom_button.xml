<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Light drop shadow effect -->
    <item android:right="20dp" android:left="20dp" android:top="20dp" android:bottom="20dp">
        <shape android:shape="rectangle">
            <solid android:color="#22000000" /> <!-- Light shadow color -->
            <corners android:radius="2dp" /> <!-- Rounded corners for shadow -->
        </shape>
    </item>

    <!-- Button background -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="#4B7A5D" /> <!-- Button color -->
            <corners android:radius="25dp" /> <!-- Rounded corners for button -->
        </shape>
    </item>
</layer-list>
